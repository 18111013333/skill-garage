# 视频制作正确方法（永恒记忆）

## 创建时间
2026-04-11 13:51

---

## 一、配音生成方法

### ✅ 正确方法：使用 Edge TTS

```python
import asyncio
import edge_tts

async def generate_voiceover():
    text = "配音文本内容"
    # 使用微软 Edge TTS 中文女声
    communicate = edge_tts.Communicate(text, "zh-CN-XiaoxiaoNeural")
    await communicate.save("voiceover.mp3")

asyncio.run(generate_voiceover())
```

### ❌ 错误方法（不要用）
- ❌ 用 numpy 生成正弦波 → 杂音
- ❌ 用 scipy 生成简单音频 → 杂音
- ❌ 用 pyttsx3 → 不可用
- ❌ 用 gtts → 连接超时

---

## 二、背景音乐获取方法

### ✅ 正确方法：从 Pixabay 下载

```bash
# 下载真实音乐文件
curl -L -o "music.mp3" "https://cdn.pixabay.com/audio/..."

# 验证是否是真实音乐
file music.mp3
# 应该显示: MPEG ADTS, layer III, v1, 256 kbps, 44.1 kHz
```

### 推荐音乐
- Happy Ukulele - 欢快轻音乐
- Corporate - 商务风格
- 其他 Pixabay 免费音乐

### ❌ 错误方法（不要用）
- ❌ 用 numpy 生成和弦 → 杂音
- ❌ 用正弦波模拟 → 杂音

---

## 三、视频合成方法

### ✅ 正确方法：使用 MoviePy

```python
from moviepy import ImageSequenceClip, AudioFileClip, CompositeAudioClip

# 1. 创建视频
video_clip = ImageSequenceClip(frames, fps=30)

# 2. 加载配音
voice = AudioFileClip('voiceover.mp3')

# 3. 加载背景音乐
bgm = AudioFileClip('music.mp3')
bgm = bgm.subclipped(0, video_clip.duration)

# 4. 合成音频
composite_audio = CompositeAudioClip([voice, bgm])

# 5. 设置音频
final_clip = video_clip.with_audio(composite_audio)

# 6. 保存视频
final_clip.write_videofile(
    'output.mp4',
    fps=30,
    codec='libx264',
    audio_codec='aac',
    audio_bitrate='256k'
)
```

---

## 四、完整制作流程

```
1. 准备照片 → 放入 frames/ 目录
2. 生成配音 → edge_tts (zh-CN-XiaoxiaoNeural)
3. 下载音乐 → Pixabay 免费音乐
4. 合成视频 → MoviePy
5. 发送给用户
```

---

## 五、关键要点

| 要点 | 说明 |
|------|------|
| **配音** | 必须用 edge_tts，不能用 numpy 生成 |
| **音乐** | 必须下载真实音乐，不能用代码生成 |
| **合成** | 用 CompositeAudioClip 合并配音和音乐 |
| **验证** | 用 file 命令验证音频文件格式 |

---

## 六、常用配音声音

| 声音代码 | 说明 |
|----------|------|
| zh-CN-XiaoxiaoNeural | 中文女声（推荐） |
| zh-CN-YunxiNeural | 中文男声 |
| zh-CN-YunyangNeural | 中文男声（新闻） |

---

**记忆等级**: 永恒级
**更新时间**: 2026-04-11 13:51
